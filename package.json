{
  "name": "global-registry-nodejs-client",
  "version": "1.0.0",
  "description": "Node.js client API for Global Registry",
  "main": "index.js",
  "scripts": {
    "lint": "standard || exit 0",
    "lint:fix": "standard --fix || exit 0",
    "test": "node --use_strict ./node_modules/jest/bin/jest --env=node --forceExit"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-eslint": "^8.2.5",
    "request-promise-native": "^1.0.5"
  },
  "standard": {
    "env": [ "jest" ]
  },
  "devDependencies": {
    "eslint": "^5.0.0",
    "jest": "^22.4.4",
    "lodash": "^4.17.5",
    "nock": "^10.0.2",
    "rewire": "^4.0.1",
    "standard": "^12.0.1",
    "uuid": "^3.3.2"
  }
}
